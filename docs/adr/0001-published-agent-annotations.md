# Published Agent Annotations

Agent Annotation changes are included in the Agent Application publishing boundary: management preview may use draft annotations, while device runtime uses the last published annotation snapshot. This prevents partially edited rich-media standard replies from becoming live on devices immediately, even though older text-only annotations previously took effect as soon as they were saved.
