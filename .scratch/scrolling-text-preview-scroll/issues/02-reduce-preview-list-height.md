# 缩减查看文本列表高度

Type: grilling
Status: resolved

## Question

查看弹窗的文本列表应如何缩减高度，既减少当前弹窗的纵向占用，又不改变标题、状态和长列表滚动行为？

## Answer

用户确认按当前文本列表最大可滚动区域的约 2/3 设置新的最大高度，即以既有 `calc(100dvh - 14rem)` 高度预算为基准再乘以 `2 / 3`。标题、状态标签、独立文本列表滚动以及始终显示的纵向滚动条均保持不变。
